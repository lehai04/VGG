{
  "name": "vgg-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:backend": "npm run dev --workspace=backend",
    "db:deploy": "npm run prisma:deploy --workspace=backend",
    "db:bootstrap-admin": "npm run admin:bootstrap --workspace=backend",
    "build": "npm run build --workspace=frontend",
    "build:admin": "npm run build --workspace=admin",
    "build:backend": "npm run build --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=admin",
    "typecheck": "npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend && npm run typecheck --workspace=admin",
    "test": "npm run test --workspace=frontend && npm run test --workspace=backend && npm run test --workspace=admin",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build && npm run build:admin"
  },
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "allowScripts": {
    "argon2@0.45.1": true,
    "prisma@7.9.1": true,
    "@prisma/engines@7.9.1": true,
    "esbuild@0.28.2": true,
    "unrs-resolver@1.12.2": true
  }
}

# VGG Admin

Ứng dụng quản trị tách biệt, chạy bằng `npm run dev:admin` tại `http://localhost:3001`.

Khung này **chưa có cơ chế xác thực**; không triển khai công khai trước khi bổ sung SSO/role-based access control và kết nối nguồn dữ liệu.

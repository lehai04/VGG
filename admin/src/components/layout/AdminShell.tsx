import Link from "next/link";
import type { ReactNode } from "react";
import { LogoutButton } from "./LogoutButton";

export function AdminShell({ admin, children }: { admin: { fullName: string; role: { name: string }; permissionCodes: string[] }; children: ReactNode }) {
  const can = (permission: string) => admin.permissionCodes.includes(permission);
  return <div className="admin-shell"><aside className="admin-sidebar"><Link className="admin-sidebar__brand" href="/admin/dashboard"><span>VGG</span><b>ADMIN</b></Link><nav className="admin-nav"><Link href="/admin/dashboard"><i>▦</i> Dashboard</Link><p>QUẢN LÝ</p>{can("admins.view") && <Link href="/admin/users"><i>♙</i> Nhân sự</Link>}{can("news.view") && <Link href="/admin/news"><i>▤</i> Tin tức</Link>}{can("resources.view") && <Link href="/admin/resources"><i>▣</i> Tài nguyên</Link>}{can("consultations.view") && <Link href="/admin/consultations"><i>◷</i> Lịch tư vấn</Link>}{can("applications.view") && <Link href="/admin/applications"><i>▧</i> Hồ sơ đăng ký</Link>}<p>HỆ THỐNG</p>{can("website.view") && <Link href="/admin/website"><i>⚙</i> Cấu hình website</Link>}<Link href="/admin/profile/security"><i>⌾</i> Tài khoản của tôi</Link></nav><div className="admin-sidebar__user"><div className="admin-avatar">{admin.fullName.slice(0,1).toUpperCase()}</div><div><strong>{admin.fullName}</strong><span>{admin.role.name}</span></div><LogoutButton /></div></aside><section className="admin-workspace"><header className="admin-topbar"><span className="admin-menu-icon">▣</span><strong>Dashboard</strong><button aria-label="Chế độ giao diện">☼</button></header><main className="admin-main">{children}</main></section></div>;
}

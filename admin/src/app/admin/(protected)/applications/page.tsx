import { backendRequest } from "@/lib/backend-api";
type Application = { id:string; fullName:string; email:string; phone:string; programme:string|null; status:string; createdAt:string };
export const metadata = { title: "Hồ sơ đăng ký" };
export default async function Page(){ const { applications } = await backendRequest<{applications:Application[]}>("/api/admin/applications"); return <div className="admin-page"><header className="admin-page__head"><div><span className="eyebrow">ADMISSIONS / APPLICATIONS</span><h1>Hồ sơ đăng ký</h1></div></header><section className="admin-card admin-table-wrap"><table className="admin-table"><thead><tr><th>Ứng viên</th><th>Chương trình</th><th>Trạng thái</th><th>Ngày gửi</th></tr></thead><tbody>{applications.map(item=><tr key={item.id}><td><strong>{item.fullName}</strong><small>{item.email} · {item.phone}</small></td><td>{item.programme||"Chưa chọn"}</td><td><span className="status-pill">{item.status}</span></td><td>{new Date(item.createdAt).toLocaleDateString("vi-VN")}</td></tr>)}</tbody></table>{!applications.length&&<p>Chưa có hồ sơ đăng ký.</p>}</section></div>}
